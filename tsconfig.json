{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode — Vite resolves modules; tsc only type-checks. */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Incremental migration: JS and TS coexist. checkJs stays off so the
       existing .js/.jsx files aren't force-checked until migrated file-by-file
       (Phase 2). New .ts/.tsx files ARE strict-checked. */
    "allowJs": true,
    "checkJs": false,
    "strict": true,
    "noFallthroughCasesInSwitch": true,

    "types": ["node"]
  },
  "comment-include": "Handler files (api/*.ts) are transpiled (type-stripped) by Vercel's esbuild, not type-checked by us; only the shared, unit-tested _lib helpers are in our typecheck scope.",
  "include": ["src", "api/_lib"]
}
