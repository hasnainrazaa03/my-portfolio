/**
 * useActiveSection.test.js
 * ---------------------------------------------------------------------------
 * Scaffold — fill in once `vitest` + `@testing-library/react` are installed.
 *
 * Run:  npm test
 */
import { describe, it, expect, vi } from 'vitest';

// Un-comment once testing-library is available:
// import { renderHook, act } from '@testing-library/react';
// import { useActiveSection } from '../hooks/useActiveSection';

describe('useActiveSection', () => {
  it.todo('defaults to "hero"');
  it.todo('updates when a new section scrolls into the spy-line');
  it.todo('does not update when the same section is still active');
  it.todo('cleans up scroll listener on unmount');
});
